
// Import from the hooks folder to match the newer shadcn structure
import { useToast, toast } from "@/hooks/use-toast";

// Re-export the toast functionality from the centralized location
export { useToast, toast };
